import { Copy } from "~commands/drag-text/copy"
import { Open } from "~commands/drag-text/open"
import { Search } from "~commands/drag-text/search"

export default [new Copy(), new Search(), new Open()]
