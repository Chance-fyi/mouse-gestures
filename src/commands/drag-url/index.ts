import { Copy } from "~commands/drag-url/copy"
import { Open } from "~commands/drag-url/open"
import { OpenWindow } from "~commands/drag-url/open-window"

export default [new Copy(), new Open(), new OpenWindow()]
