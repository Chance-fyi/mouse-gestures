export enum Group {
  Gesture = "gesture"
}

export enum ConfigType {
  Input = "input",
  Select = "select",
  Toggle = "toggle"
}

export enum Direction {
  Up = "up",
  Down = "down",
  Left = "left",
  Right = "right"
}

export enum Position {
  Left = "left",
  Right = "right",
  Home = "home",
  End = "end"
}
