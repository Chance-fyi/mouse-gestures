export enum Group {
  Gesture = "gesture",
  DragText = "drag_text",
  DragUrl = "drag_url",
  DragImage = "drag_image"
}

export enum ConfigType {
  Input = "input",
  Textarea = "textarea",
  Select = "select",
  Toggle = "toggle"
}

export enum Direction {
  Up = "up",
  Down = "down",
  Left = "left",
  Right = "right"
}

export enum Position {
  Left = "left",
  Right = "right",
  Home = "home",
  End = "end"
}
